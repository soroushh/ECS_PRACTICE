flaskapp:
  build:
    image: flask_app
    dockerfile_path: ./flaskapp-docker/Dockerfile

nginx:
  build: nginx
  dockerfile_path: ./nginx/Dockerfile

dockercfgGenerator:
  image: codeship/aws-ecr-dockercfg-generator
  add_docker: true
  encrypted_env_file:
    - env.encrypted
