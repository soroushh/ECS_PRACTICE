flaskapp:
  build: ./flaskapp-docker

nginx:
  build: ./nginx
  links:
    - flaskapp

dockercfgGenerator:
  image: codeship/aws-ecr-dockercfg-generator
  add_docker: true
  encrypted_env_file:
    - env.encrypted
